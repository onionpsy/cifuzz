allprojects {
    apply plugin: "java"
    apply plugin: "jacoco"

    repositories {
        mavenCentral()
    }

    tasks.register("printClasspath") {
        println "cifuzz.test.classpath=${sourceSets.test.runtimeClasspath.asPath}";
    }
}
